import styled from "styled-components";

const Wrapper = styled.span`
  position: absolute;
  margin-top: 8px;
  margin-right: 5px;
  justify-self: end;
  padding: 10px;
  z-index: 2;
`;

export default Wrapper;
