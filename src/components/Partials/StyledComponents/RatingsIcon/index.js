import styled from "styled-components";

export const Wrapper = styled.span`
  margin: 10px 0 10px 0px;
  display: inline-block;
`;

export const ExtendedWrapper = Wrapper.extend`
  margin: 0 0 10px 0px;
  display: inline-block;
`;
